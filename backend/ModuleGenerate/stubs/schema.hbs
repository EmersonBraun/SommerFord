import { schema{{#if module.relations}}, rules {{/if}}} from '@ioc:Adonis/Core/Validator'

export const {{module.name.pascalCase}}Schema = schema.create({
{{#each module.fields}}
  {{module.fields.name}}: schema.{{module.fields.type}}(),
{{/each}}
{{#if module.relations}}
{{#each module.relations}}
  {{module.relations.field}}: schema.{{module.relations.type}}([
    rules.exists({ table: '{{module.relations.name}}', column: '{{module.relations.relationedField}}' }),
  ]),
{{/each}}
{{/if}}
})